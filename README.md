### ⚠️ Notice: Master Branch Sunset

The `master` branch has been **sunset** and will **no longer receive updates or maintenance**.
Please switch to the `stable` branch for the latest **stable versions** of the plugins.
You may also use the `dev` branch for development versions, but note that it **may not always be compatible** with the stable releases of **vspreview**.
